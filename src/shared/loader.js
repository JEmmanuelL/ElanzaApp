// Shared Loader component logic

export const showLoader = (containerSelector = 'body') => {
    console.log('Showing loader in', containerSelector);
    // Add loader to DOM
};

export const hideLoader = (containerSelector = 'body') => {
    console.log('Hiding loader in', containerSelector);
    // Remove loader from DOM
};

